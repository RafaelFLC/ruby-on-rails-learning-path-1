<%= form_for(@user, :url => "/admin/users/#{@user.id}", :method => 'delete') do |f| %>
<h3>Are you sure to delete <%= @user.name %> ?</h3>
<%= link_to '< back to list', admin_users_path %> <br/> <br/>

<%= f.submit "Yes, Delete it", disable_with: 'Submiting...' %>
<% end %>